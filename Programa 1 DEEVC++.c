	#include<stdio.h>
	#include<stdlib.h>
	#include<locale.h>
	   // Biblioteca padrão para entrada e saída (ex: printf e scanf)
	  // Fornece funções de alocação de memória, controle de processos e outras
	 // Permite configurar a acentuação e regionalização (ex: usar português)
	// O comando #include avisa ao compilador que vamos usar recursos dessas bibliotecas.

	int main (){
		setlacale(LC_ALL,"portuguese")
		int x;
		float y;
		printf( "Tabuada de adição\n");
		for(x=1;x<=10;x++){
		printf("\n+%d = %d",x,1x);	
			
		}
	
	
	
	
}


